{
  "private": true,
  "name": "microfrontend",
  "version": "1.0.0",
  "description": "Template microfrontend",
  "author": "Luis Diego",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel --no-cache",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build": "turbo run build",
    "deploy": "turbo run deploy",
    "serve": "turbo run serve --parallel --no-cache"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.0.2",
    "@commitlint/config-conventional": "^17.0.2",
    "husky": "^8.0.1",
    "turbo": "^1.2.16"
  }
}
